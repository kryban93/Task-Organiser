import React from 'react';
import Nav from '../../components/Nav/Nav';

const SettingsView = () => {
  return (
    <section>
      <p>settings view</p>
      <Nav />
    </section>
  );
};

export default SettingsView;
