import React from 'react';
import Nav from '../../components/Nav/Nav';

const TasksView = () => {
  return (
    <section>
      <p>Tasks view</p>
      <Nav />
    </section>
  );
};

export default TasksView;
